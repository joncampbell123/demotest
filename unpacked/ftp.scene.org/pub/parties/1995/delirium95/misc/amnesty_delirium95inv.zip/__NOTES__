Notes:
- Demo ASSUMES flat real mode is active, without any attempt to set it up. It will crash if 64KB segment limits are enforced.
Check:
- Two of the music samples sound like glitch audio, is that intentional?
