Why the demo failed the test:
- Demo runs, displays "guns" and rotating object, then crashes.
  The reason is that DOSBox SVN does not emulate Long Filenames
  and the demo cannot access it's data files without it.
