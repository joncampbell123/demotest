Why the demo failed the test:
- Demo exits to DOS, but then hitting a key causes a crash.
Notes:
- GUS music playback (or the demo itself?) seems to read and write random I/O ports during the demo. DOSLIB utility to patch out that bug is available.
